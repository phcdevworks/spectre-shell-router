# Spectre Shell Router — Execution Todo

No active work is currently open in this package. Phases 1 through 4 are
complete — see [ROADMAP.md](ROADMAP.md) for the full delivery history and
[CHANGELOG.md](CHANGELOG.md) for release-by-release detail.

New routing surface here remains demand-driven: it opens only when a
downstream consumer (`spectre-init`, `spectre-shell`) or a vendor surfaces a
concrete missing routing requirement.

When a new phase opens, add it here with the same P0/P1/P2 structure the
completed phases used in `ROADMAP.md`.
